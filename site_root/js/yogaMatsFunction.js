function displayImage(elem) {
    var image = document.getElementById("canvas");
    image.src = elem.value;        
  }

  function displayImageTwo(elem) {
    var image = document.getElementById("canvastwo");
    image.src = elem.value;        
  }